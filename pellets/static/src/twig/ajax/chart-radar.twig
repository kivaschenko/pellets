      {% include '../content/_chart-radar.twig' %}

<script>
  var plugins = [
    '{{ folder.plugins }}chart.js/Chart.min.js',
  ]
  App.loadPlugins(plugins, null).then(() => {
    {% include '../content/_chart-radar.js' %}

  }).then(() => App.stopLoading())
</script>
